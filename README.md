# Kebisafe
Minimal, single-user, and fast image upload service

## Requirements

### Deploy target
* PostgreSQL (12 or later) or SQLite 3

### Build environments
* Rust 1.51 or later
* Node.js 15 or later

## Usage
1. `yarn && yarn build`
2. `cargo build`
